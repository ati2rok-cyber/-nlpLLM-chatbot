#!/usr/bin/env python3

import os
import json
import streamlit as st
from groq import Groq
from dotenv import load_dotenv

# โหลด environment variables จากไฟล์ .env
load_dotenv(".env")
GROQ_API_KEY = os.getenv("GROQ_API_KEY")
client = Groq(api_key=GROQ_API_KEY)

def chatboot(question):
    system_prompt = """
AI-Powered Ad Checker for FDA (Thailand) Regulations
language: "Thai"


role: |
คุณคือ ***ผู้เชี่ยวชาญด้านการตรวจสอบโฆษณาตามกฎหมาย อย.*** คุณได้รับมอบหมายให้ตรวจสอบและวิเคราะห์ข้อความโฆษณาที่เกี่ยวข้องกับ ***อาหารและเครื่องสำอาง*** โดยมีวัตถุประสงค์เพื่อระบุ ***คำโฆษณาเกินจริง, เป็นเท็จ, หรือหลอกลวง*** ที่ผิดกฎหมายและอาจเป็นอันตรายต่อผู้บริโภค

instruction: |
**ขอบเขตและข้อกำหนด:**
1.  **การวิเคราะห์**: คุณต้องวิเคราะห์ข้อความอย่างละเอียดเพื่อหา ***คำหรือวลีที่ละเมิดกฎหมาย*** ตามที่กำหนดโดย อย.
2.  **ความแม่นยำ**: การตัดสินใจของคุณต้อง **ตรงไปตรงมา** และ **อ้างอิงจากหลักเกณฑ์ที่กำหนดเท่านั้น** โดย **ห้าม** สร้างข้อมูลที่ไม่เป็นความจริง (AI Hallucination)
3.  **การระบุประเภทผลิตภัณฑ์**: คุณต้องระบุให้ได้ว่าข้อความโฆษณาที่ได้รับเป็นของผลิตภัณฑ์ประเภท **"อาหาร"** หรือ **"เครื่องสำอาง"**
4.  **การให้เหตุผล (Chain of Thought)**: ทุกการตัดสินใจของคุณต้องมี **"เหตุผล"** ที่ชัดเจน โดยอธิบายว่าทำไมจึงจัดประเภทข้อความนั้น ๆ ว่าเป็นโฆษณาที่ผิดกฎหมายหรือไม่ผิดกฎหมายตามกฎของ อย.
5.  **รูปแบบคำตอบ**: คุณต้องตอบกลับเป็น JSON format เท่านั้น ห้ามตอบนอกเหนือจาก schema นี้.

**หลักเกณฑ์ที่ต้องตรวจสอบ (สำหรับ Few-shots):**

* **หมวดหมู่อาหาร**:
    * **ห้าม** อ้างสรรพคุณในการ **"รักษาโรค"** เช่น "ผลิตภัณฑ์นี้รักษาโรคมะเร็งได้" หรือ "ช่วยป้องกันโรคเบาหวานได้"
    * **ห้าม** อ้างสรรพคุณในการ **"ลดความอ้วน"** เช่น "ทานแล้วผอมทันที 10 กิโลใน 1 เดือน" หรือ "บล็อกแป้งและไขมันได้ 100%"
    * **ห้าม** อ้างสรรพคุณในการ **"เพิ่มสมรรถภาพทางเพศ"** เช่น "ช่วยเพิ่มขนาด" หรือ "เห็นผลภายใน 5 นาที"
    * **ห้าม** อ้างสรรพคุณในการ **"บำรุงผิวพรรณ"** ที่เกินจริง เช่น "ผิวขาวใสใน 3 วัน"
* **หมวดหมู่เครื่องสำอาง**:
    * **ห้าม** อ้างสรรพคุณที่ **"เกินจริง"** เช่น "ทำให้หน้าขาวขึ้นอย่างรวดเร็ว"
    * **ห้าม** อ้างสรรพคุณที่ **"รักษาโรค"** เช่น "ลดสิวอักเสบภายใน 7 วัน" (สิวอักเสบถือเป็นอาการป่วย) หรือ "รักษาสิวฝ้าหายขาด"

examples: **ตัวอย่าง (Few-shots):**

**ตัวอย่างที่ 1:**
* **ข้อความโฆษณา**: "ครีมหน้าขาวตัวใหม่! ทาปุ๊บ ขาวใสทันทีภายใน 3 วัน ช่วยให้ฝ้าจางลงอย่างถาวร"
* **คำตอบที่ต้องการ**:
    ```json
    {
      "ผลิตภัณฑ์": "เครื่องสำอาง",
      "การตัดสิน": "ผิดกฎหมาย",
      "เหตุผล": "มีการใช้คำว่า \"ขาวใสทันทีภายใน 3 วัน\" ซึ่งเป็นการอ้างสรรพคุณที่เกินจริงและไม่สามารถเป็นไปได้ตามหลักความเป็นจริงของเครื่องสำอาง และการกล่าวอ้างว่า \"ฝ้าจางลงอย่างถาวร\" เป็นการกล่าวอ้างที่เกินขอบเขตการใช้งานของเครื่องสำอางซึ่งถือเป็นการโฆษณาที่ผิดกฎหมายตามกฎของ อย."
    }
    ```

**ตัวอย่างที่ 2:**
* **ข้อความโฆษณา**: "อาหารเสริมลดน้ำหนัก Slim Xtreme! ทานวันละเม็ด ผอมทันใจ 15 กิโลใน 2 สัปดาห์"
* **คำตอบที่ต้องการ**:
    ```json
    {
      "ผลิตภัณฑ์": "อาหาร",
      "การตัดสิน": "ผิดกฎหมาย",
      "เหตุผล": "มีการใช้คำว่า \"ผอมทันใจ 15 กิโลใน 2 สัปดาห์\" ซึ่งเป็นการอ้างสรรพคุณในการลดความอ้วนที่เกินจริงและผิดกฎหมายตามข้อกำหนดของ อย."
    }
    ```

**ตัวอย่างที่ 3:**
* **ข้อความโฆษณา**: "ครีมกันแดด SPF50+ สูตรพิเศษจากธรรมชาติ ช่วยปกป้องผิวจากแสงแดด"
* **คำตอบที่ต้องการ**:
    ```json
    {
      "ผลิตภัณฑ์": "เครื่องสำอาง",
      "การตัดสิน": "ไม่ผิดกฎหมาย",
      "เหตุผล": "ข้อความโฆษณานี้เป็นไปตามมาตรฐานการโฆษณาปกติของครีมกันแดด และไม่มีการกล่าวอ้างสรรพคุณเกินจริงที่ผิดกฎหมายตามหลักเกณฑ์ของ อย."
    }
    ```

"""

    response = client.chat.completions.create(
        model="llama-3.1-8b-instant",
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": question},
        ],
        temperature=0.2,
        max_tokens=1024,
    )
    return response.choices[0].message.content.strip()


def main():
    st.set_page_config(page_title="แชตบอตตรวจสอบโฆษณา อย.", page_icon="📝", layout="wide")
    st.markdown("""
        <style>
        body { background-color: #f0f2f6; }
        .stForm { background-color: #ffffff; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); }
        </style>
    """, unsafe_allow_html=True)

    st.title("📝 แชตบอตตรวจสอบโฆษณาตามกฎหมาย อย.")
    st.subheader("โปรดพิมพ์ข้อความโฆษณาเพื่อตรวจสอบว่ามีเนื้อหาเกินจริงหรือผิดกฎหมายหรือไม่")

    if "messages" not in st.session_state:
        st.session_state["messages"] = [
            {"role": "assistant", "content": "สวัสดีค่ะ ฉันสามารถช่วยตรวจสอบข้อความโฆษณาอาหารและเครื่องสำอางได้ค่ะ กรุณาพิมพ์ข้อความที่ต้องการตรวจสอบได้เลยค่ะ"}
        ]

    with st.form("chat_form"):
        query = st.text_input("🗣️ ข้อความโฆษณา:", placeholder="พิมพ์ข้อความโฆษณาที่ต้องการตรวจสอบ...").strip()
        submitted = st.form_submit_button("🚀 ส่งข้อความ")

        if submitted and query:
            answer = chatboot(query)
            st.session_state["messages"].append({"role": "user", "content": query})
            st.session_state["messages"].append({"role": "assistant", "content": answer})
        elif submitted and not query:
            st.warning("⚠️ กรุณาพิมพ์ข้อความก่อนส่ง")

    for msg in st.session_state["messages"]:
        if msg["role"] == "user":
            st.markdown(f"**🧑‍💻 คุณ:** {msg['content']}")
        else:
            try:
                data = json.loads(msg["content"])
                st.markdown("---")
                st.markdown("#### ✅ ผลการตรวจสอบ")
                st.markdown(f"**ประเภทผลิตภัณฑ์:** {data.get('ผลิตภัณฑ์', 'ไม่ระบุ')}")
                st.markdown(f"**สถานะ:** **`{data.get('การตัดสิน', 'ไม่ระบุ')}`**")
                st.info(f"**เหตุผล:** {data.get('เหตุผล', 'ไม่ระบุ')}")
            except json.JSONDecodeError:
                st.markdown(f"**🤖 บอท:** {msg['content']}")
            except Exception as e:
                st.markdown(f"**🤖 บอท:** เกิดข้อผิดพลาดในการประมวลผล: {e}")

if __name__ == "__main__":
    main()

